# use  direct connection with either server or localhost for database instead of using image.
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env  # 👈 Load .env into the container
